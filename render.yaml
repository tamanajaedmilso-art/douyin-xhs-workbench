services:
  - type: web
    name: douyin-xhs-workbench-backend
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: API_KEY
        generateValue: true
      - key: FRONTEND_ORIGINS
        value: ''
